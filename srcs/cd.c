#include "minishell.h"

int get_flag_cd(char **av, int *flag)
{
	// while ();
}

int cd(char *argv[], char *envp[], t_data *data)
{
	// char *tmp;
	// int flag;

	// tmp = NULL;
	// flag = 0;
	// ft_strcpy(tmp, data->old_dir);
	// getcwd(data->old_dir, PATH_MAX);
	// else if (!argv[1] || ft_strequ(argv[1], '~' && !argv[2]))
	// 	tmp = get_envvar(envp, "HOME=");
	// else if ((ft_strequ(argv[1], '-') || ft_strequ(argv[1], '--'))&& !argv[2]))
	// 	tmp = get_envvar(envp, "OLDPWD=");
	// else
	// {
	// 	argv += get_flag_cd(argv + 1, &flag);
	// }

	return (1);
}
